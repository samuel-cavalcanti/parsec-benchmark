#!/bin/bash

# gcc-hooks.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/apps/freqmine/parsec/gcc.bldconf

# Package dependencies
build_deps="$build_deps pascal-releases"
LIBS="${LIBS} -lmanualinst"
